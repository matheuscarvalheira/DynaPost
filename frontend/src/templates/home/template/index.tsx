import { ReactNode } from "react";
import * as S from './styles';

// Usar como template para criar templates/seções
export const Template = (): ReactNode => {
  return (
    <S.Template>
      Template
    </S.Template>
  )
}