import { ReactNode } from "react";
import * as S from './styles'
import { Button } from "@/components/button";

export const Cursos = (): ReactNode => {
  return (
    <S.Cursos>
      cirar conta
    </S.Cursos>
  )
}